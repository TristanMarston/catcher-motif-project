import LandingPage from '@/_components/LandingPage';

const page = () => {
    return (
        <div className='min-w-full min-h-screen flex items-center justify-center bg-gradient overflow-y-scroll'>
            <LandingPage />
        </div>
    );
};

export default page;
